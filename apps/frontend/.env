# Frontend environment — Next.js reads .env from its own app directory.
# Phase 37.3: empty = relative URLs (same-origin via Next dev rewrite in
# dev, nginx in prod). Override only if backend is on a different origin.
NEXT_PUBLIC_API_URL=
