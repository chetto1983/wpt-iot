import { describe, it } from 'vitest';

describe('energy milestone e2e', () => {
  it.todo('configure settings -> lock baseline -> drive deterministic cycle data or simulator scenario -> verify dashboard + cycles + reconciliation -> generate PDF twice and compare');
  it.todo('scenario hook: counter reset handling keeps milestone assertions deterministic');
  it.todo('scenario hook: OFF gap degraded-quality path is explicit in the milestone test');
  it.todo('scenario hook: month boundary continuity stays covered in the milestone test');
  it.todo('scenario hook: baseline overlap rejection stays covered in the milestone test');
});
